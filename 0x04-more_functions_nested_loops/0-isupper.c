#include "main"
/**
* _isupper - check alphabet uppercase
* @c: input
* Return: return 1 if upper otherwise Always 0 (Success)
*/





int _isupper(int c)
{
if (c >= 65 && c <= 90)
return (1);
else
return (0);
}
